package com.paypal.challenge.ViewModel

class LoginViewModel
{

    fun validateLogin(username:String, password:String)
    {
        if((username.isNotBlank() || username.isNotEmpty()) && (password.isNotBlank() || password.isNotEmpty()))
        {
            username.
        }
    }
}